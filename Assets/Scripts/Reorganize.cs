using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reorganize : MonoBehaviour
{
    // Start is called before the first frame update
    void OnClick() {

        // first checks for objects in the command position (bed, then desk)
        foreach (var sublist in FurnitureManager.furnitureItems) {
            
        }
    }
}
